'use client'; import {useState} from 'react';
export default function CreateEventForm(){const [open,setOpen]=useState(false),[msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const b=Object.fromEntries(f.entries());const r=await fetch('/api/events',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(b)});const d=await r.json();if(!r.ok){setMsg(d.error||'Failed');return}setMsg('Event created successfully');e.currentTarget.reset();}return <div className="card p-5"><button onClick={()=>setOpen(!open)} className="rounded-xl bg-indigo-600 px-4 py-2.5 font-semibold text-white">{open?'Close':'Create event'}</button>{open&&<form onSubmit={submit} className="mt-5 grid gap-3 md:grid-cols-2"><input name="title" placeholder="Event title" required className="rounded-xl border p-3"/><input name="category" placeholder="Category" required className="rounded-xl border p-3"/><input name="venue" placeholder="Venue" required className="rounded-xl border p-3"/><input name="capacity" type="number" placeholder="Capacity" required className="rounded-xl border p-3"/><input name="startDate" type="datetime-local" required className="rounded-xl border p-3"/><input name="endDate" type="datetime-local" required className="rounded-xl border p-3"/><input name="registrationDeadline" type="datetime-local" required className="rounded-xl border p-3"/><input name="registrationFee" type="number" defaultValue="0" className="rounded-xl border p-3"/><textarea name="description" placeholder="Description" required className="min-h-24 rounded-xl border p-3 md:col-span-2"/><input name="eligibility" placeholder="Eligibility" className="rounded-xl border p-3 md:col-span-2"/><select name="status" className="rounded-xl border p-3"><option value="DRAFT">Draft</option><option value="PUBLISHED">Published</option></select><button className="rounded-xl bg-slate-900 px-4 py-3 font-semibold text-white">Save event</button>{msg&&<p className="text-sm text-slate-500 md:col-span-2">{msg}</p>}</form>}</div>}
