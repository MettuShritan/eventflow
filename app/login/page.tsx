"use client"; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState('participant@eventflow.demo'),[password,setPassword]=useState('EventFlow@123'),[error,setError]=useState(''),[loading,setLoading]=useState(false);const r=useRouter();async function submit(e:any){e.preventDefault();setLoading(true);const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const d=await res.json();if(!res.ok){setError(d.error||'Login failed');setLoading(false);return}r.push(d.redirect)}return <div className="gradient-bg flex min-h-screen items-center justify-center px-4"><form onSubmit={submit} className="card w-full max-w-md p-8"><Link href="/" className="text-sm font-semibold text-indigo-600">← EventFlow</Link><h1 className="mt-6 text-3xl font-black">Welcome back</h1>{error&&<div className="mt-4 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}<div className="mt-6 space-y-4"><label className="block text-sm font-medium">Email<input value={email} onChange={e=>setEmail(e.target.value)} className="mt-1 w-full rounded-xl border px-3 py-3" required/></label><label className="block text-sm font-medium">Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="mt-1 w-full rounded-xl border px-3 py-3" required/></label><button disabled={loading} className="w-full rounded-xl bg-indigo-600 py-3 font-bold text-white">{loading?'Signing in…':'Sign in'}</button></div><div className="mt-6 rounded-xl bg-slate-50 p-4 text-xs text-slate-600">participant@eventflow.demo · committee@eventflow.demo · admin@eventflow.demo<br/>Password: EventFlow@123</div><p className="mt-5 text-center text-sm">Need an account? <Link href="/register" className="font-bold text-indigo-600">Register</Link></p></form></div>}
