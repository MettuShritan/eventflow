"use client"; import {useState} from 'react'; import {useRouter} from 'next/navigation'; export default function Form({eventId,user}:{eventId:string;user:any}){const [terms,setTerms]=useState(false),[error,setError]=useState('');const r=useRouter();async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch('/api/registrations',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({eventId,fullName:f.get('fullName'),email:f.get('email'),phone:f.get('phone'),college:f.get('college'),department:f.get('department'),year:f.get('year'),additionalInfo:f.get('additionalInfo'),terms})});const d=await res.json();if(!res.ok){setError(d.error);return}r.push('/participant/registrations')}return <form onSubmit={submit} className="card p-6"><h2 className="text-xl font-bold">Register</h2>{error&&<p className="mt-3 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<div className="mt-5 space-y-3"><input name="fullName" defaultValue={user.name} required className="w-full rounded-xl border p-3" placeholder="Full name"/><input name="email" defaultValue={user.email} required className="w-full rounded-xl border p-3" placeholder="Email"/><input name="phone" className="w-full rounded-xl border p-3" placeholder="Phone"/><input name="college" defaultValue={user.college||''} className="w-full rounded-xl border p-3" placeholder="College"/><input name="department" defaultValue={user.department||''} className="w-full rounded-xl border p-3" placeholder="Department"/><input name="year" type="number" className="w-full rounded-xl border p-3" placeholder="Year"/><textarea name="additionalInfo" className="min-h-20 w-full rounded-xl border p-3" placeholder="Additional information"/><label className="flex gap-2 text-sm"><input type="checkbox" checked={terms} onChange={e=>setTerms(e.target.checked)}/>I agree to the terms.</label><button disabled={!terms} className="w-full rounded-xl bg-indigo-600 py-3 font-bold text-white">Confirm registration</button></div></form>}