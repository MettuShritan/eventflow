import {getCurrentUser} from '@/lib/auth'; import {Shell} from '@/components/ui'; export default async function P(){const u=await getCurrentUser();return <Shell role="PARTICIPANT" title="Profile"><div className="card max-w-2xl p-6"><h2 className="text-xl font-bold">{u!.name}</h2><p className="mt-2 text-slate-500">{u!.email}</p><div className="mt-5 grid gap-3 md:grid-cols-2"><Info a="College" b={u!.college||'—'}/><Info a="Department" b={u!.department||'—'}/><Info a="Year" b={String(u!.year||'—')}/><Info a="Role" b={u!.role}/></div></div></Shell>} function Info({a,b}:{a:string;b:string}){return <div className="rounded-xl bg-slate-50 p-4"><small>{a}</small><p className="font-semibold">{b}</p></div>}