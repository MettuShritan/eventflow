import {getCurrentUser} from '@/lib/auth'; import {prisma} from '@/lib/prisma'; import {Shell} from '@/components/ui'; export default async function N(){const u=await getCurrentUser();const ns=await prisma.notification.findMany({where:{userId:u!.id},orderBy:{createdAt:'desc'}});return <Shell role="ADMIN" title="Notifications"><div className="card divide-y">{ns.map(n=><div className="p-5" key={n.id}><b>{n.title}</b><p className="mt-1 text-sm text-slate-500">{n.message}</p></div>)}</div></Shell>}