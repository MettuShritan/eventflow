"use client"; import {useState} from 'react'; export default function F(){const [m,setM]=useState('');async function go(e:any){e.preventDefault();const r=await fetch('/api/admin/users',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget).entries()))});const d=await r.json();setM(r.ok?'Created':d.error||'Failed');if(r.ok)e.currentTarget.reset()}return <form onSubmit={go} className="card mb-5 grid gap-3 p-5 md:grid-cols-4"><input name="name" required placeholder="Name" className="rounded-xl border p-3"/><input name="email" required type="email" placeholder="Email" className="rounded-xl border p-3"/><input name="password" required placeholder="Password" className="rounded-xl border p-3"/><select name="role" className="rounded-xl border p-3"><option>EVENT_COMMITTEE</option><option>PARTICIPANT</option><option>ADMIN</option></select><button className="rounded-xl bg-indigo-600 px-4 py-3 font-semibold text-white md:col-span-4">Create account</button>{m&&<p className="text-sm">{m}</p>}</form>}