"use client"; import {useState} from 'react'; import {Shell} from '@/components/ui'; export default function A(){const [code,setCode]=useState(''),[msg,setMsg]=useState('');async function go(e:any){e.preventDefault();const r=await fetch('/api/committee/attendance',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({registrationNumber:code})});const d=await r.json();setMsg(r.ok?`Checked in ${d.participant.name} for ${d.event.title}`:d.error)}return <Shell role="EVENT_COMMITTEE" title="Attendance"><div className="card mx-auto max-w-xl p-8"><h2 className="text-2xl font-bold">QR check-in</h2><p className="mt-2 text-sm text-slate-500">Enter the registration reference from the participant QR.</p><form onSubmit={go} className="mt-5 flex gap-2"><input value={code} onChange={e=>setCode(e.target.value)} className="flex-1 rounded-xl border p-3" placeholder="EF-..."/><button className="rounded-xl bg-indigo-600 px-5 font-semibold text-white">Verify</button></form>{msg&&<p className="mt-4 rounded-xl bg-slate-50 p-4 text-sm">{msg}</p>}</div></Shell>}