import {getCurrentUser} from '@/lib/auth'; import {prisma} from '@/lib/prisma'; import {Shell,Badge} from '@/components/ui'; export default async function R(){const u=await getCurrentUser();const rs=await prisma.registration.findMany({where:{event:{committees:{some:{userId:u!.id}}}},include:{event:true,participant:true},orderBy:{registeredAt:'desc'}});return <Shell role="EVENT_COMMITTEE" title="Registrations"><div className="card overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b bg-slate-50"><th className="p-4">Participant</th><th className="p-4">Event</th><th className="p-4">Status</th></tr></thead><tbody>{rs.map(r=><tr className="border-b" key={r.id}><td className="p-4">{r.participant.name}<div className="text-xs text-slate-400">{r.participant.email}</div></td><td className="p-4">{r.event.title}</td><td className="p-4"><Badge tone="green">{r.status}</Badge></td></tr>)}</tbody></table></div></Shell>}