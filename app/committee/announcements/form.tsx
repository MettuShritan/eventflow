"use client"; import {useState} from 'react'; export default function F({events}:{events:any[]}){const [m,setM]=useState('');async function go(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/committee/announcements',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});const d=await r.json();setM(r.ok?'Published':d.error)}return <form onSubmit={go} className="card p-6"><h2 className="font-bold">New announcement</h2><div className="mt-4 space-y-3"><select name="eventId" className="w-full rounded-xl border p-3">{events.map(e=><option key={e.id} value={e.id}>{e.title}</option>)}</select><input name="title" required placeholder="Title" className="w-full rounded-xl border p-3"/><textarea name="message" required placeholder="Message" className="min-h-24 w-full rounded-xl border p-3"/><button className="rounded-xl bg-indigo-600 px-5 py-3 font-semibold text-white">Publish</button>{m&&<p className="text-sm">{m}</p>}</div></form>}