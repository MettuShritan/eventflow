"use client"; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Register(){const [form,setForm]=useState({name:'',email:'',password:'',college:'',department:''}),[error,setError]=useState('');const r=useRouter();async function submit(e:any){e.preventDefault();const res=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});const d=await res.json();if(!res.ok){setError(d.error||'Registration failed');return}r.push('/participant')}return <div className="gradient-bg flex min-h-screen items-center justify-center px-4"><form onSubmit={submit} className="card w-full max-w-lg p-8"><Link href="/" className="text-sm font-semibold text-indigo-600">← EventFlow</Link><h1 className="mt-6 text-3xl font-black">Create participant account</h1>{error&&<div className="mt-4 rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</div>}<div className="mt-6 grid gap-4 md:grid-cols-2">{[['name','Full name'],['email','Email'],['password','Password'],['college','College / organization'],['department','Department']].map(([k,l])=><label key={k} className="text-sm font-medium">{l}<input type={k==='password'?'password':'text'} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})} className="mt-1 w-full rounded-xl border px-3 py-3" required={['name','email','password'].includes(k)}/></label>)}</div><button className="mt-6 w-full rounded-xl bg-indigo-600 py-3 font-bold text-white">Create account</button></form></div>}
