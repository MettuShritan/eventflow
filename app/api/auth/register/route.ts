import {NextResponse} from 'next/server'; import {prisma} from '@/lib/prisma'; import {hashPassword,createSession} from '@/lib/auth'; export async function POST(req:Request){try{const b=await req.json();if(!b.name||!b.email||!b.password||b.password.length<8)return NextResponse.json({error:'Name, email and 8+ character password are required'},{status:400});const email=String(b.email).toLowerCase();if(await prisma.user.findUnique({where:{email}}))return NextResponse.json({error:'Email already registered'},{status:409});const u=await prisma.user.create({data:{name:b.name,email,passwordHash:await hashPassword(b.password),role:'PARTICIPANT',college:b.college||null,department:b.department||null}});await createSession(u);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Invalid request'},{status:400})}}